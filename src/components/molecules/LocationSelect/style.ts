import styled from "styled-components";

export const Content = styled.div`
display: flex;
padding: 0;

& >input {
  border: none;
  width: 100%;
  text-align: center;
}

& > div {
  margin: 0;
  border-top: 0;
}
`